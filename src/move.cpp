#include "move.h"
#include "utils.h"
#include "logger.h"

bool knight(int from_index, int to_index)
{
  Square from_square = index_to_file_rank(from_index);
  Square to_square = index_to_file_rank(to_index);

  return true;
};